#include <iostream>
#include <cmath>
using namespace std;

int main() {
	int a, b, c;
	
	cout << "Quadratic Discriminant\n";

	cout << "\nWhat is the value of the first coefficient? (a) :";
	cin >> a;

	cout << "\nWhat is the value of the second coefficient? (b): ";
	cin >> b;

	cout << "\nWhat is the value of the third coefficient? (c): ";
	cin >> c; 


	return 0;
}